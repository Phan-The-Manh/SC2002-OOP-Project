package service;

public interface HDBManagerService {
    boolean checkLogin(String userId, String password);
    // void createProject(HDBManager manager, BTOProject project);
    // void editProject(HDBManager manager, BTOProject project, String newProjectName, String newNeighborhood);
    // void deleteProject(HDBManager manager, BTOProject project);
    // void toggleProjectVisibility(HDBManager manager, BTOProject project, boolean visibilityStatus);
    // void manageHDBOfficerRegistration(HDBManager manager, BTOProject project, HDBOfficer officer, boolean approve);
    // void manageApplicantApplication(HDBManager manager, BTOProject project, BTOApplication application, boolean approve);
    // void manageWithdrawalRequest(BTOApplication application, boolean approve);
    // void generateReport(HDBManager manager, BTOProject project, String filterCriteria);
    // void viewEnquiries(HDBManager manager, BTOProject project);
    // void replyToEnquiry(HDBManager manager, Enquiry enquiry, String replyText);
}